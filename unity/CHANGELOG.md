# Changelog — DKMads DMP Unity SDK

All notable changes to UPM package `com.dkmads.dmp` are documented here.

## [0.1.0] — 2026-06-18

### Added

- `DKMadsDMP` runtime (`Init`, `Identify`, `Track`, `SetTraits`, `SetContext`, `Flush`, etc.)
- `GetSharedIdentity()` for SSP bid-time eval handoff
- `Demographics` helpers
- Sample: `Samples~/IdentityHandoff` — minimal SSP identity wiring

### Pending (0.2.0)

- OpenUPM / scoped registry publish
- Play Mode integration test against local ingest
